<h1>Profiles#index</h1>


 <ul>
    <% @profiles.each do |profile| %>
    <li><%= link_to "#{profile.name}", profile_path(profile), :class=> "btn btn-default" %>:<%=%></li>
    <% end %>

 </ul>

<%= link_to "Add a New profile", new_profile_path, :class=> "btn btn-default" %>

